doctype html

html(lang="en")

  head
    meta(charset='utf-8')
    meta(http-equiv='X-UA-Compatible', content='IE=edge')
    meta(name='viewport', content='width=device-width, initial-scale=1')
    title Events@LaStatale
    meta(name='description', content='Events organized/hosted by University of Milan')
    base(href=(webpackConfig.output.publicPath || '/')+htmlWebpackPlugin.options.filename)

    //- The CSS file(s) of the application will be automagically injected here

  body

    div(class=htmlWebpackPlugin.options.cssStylePrefix)
      .header-container
        .page-header
          h1 Events<span class="inverse-highlight">@</span>LaStatale
      #app(style="max-width: 1024px; margin-left: auto; margin-right: auto;")

    //- The JS file(s) of the application will be automagically injected here
