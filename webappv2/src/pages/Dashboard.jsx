import React from 'react';

function Dashboard(props) {
  return (
    <>
      <h1>
        Dashboard
      </h1>
      {/*
      <p>
        <b>Props keys:</b> {JSON.stringify(Object.keys(props))}
      </p>
      <p>
        <b>Props:</b>
      </p>
      <pre style={{ width: '100%' }}>{JSON.stringify(props, null, 2)}</pre>
      */}
    </>
  );
}

export default Dashboard;